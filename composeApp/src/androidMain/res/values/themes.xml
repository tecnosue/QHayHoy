<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema principal de la app, sin ActionBar -->
    <style name="Theme.QHayHoy" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/splash_background</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!-- Tema del splash screen, hereda al principal después -->
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.QHayHoy</item>
    </style>
</resources>